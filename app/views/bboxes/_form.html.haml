= form_with(model: bbox, local: true) do |form|
  - if bbox.errors.any?
    #error_explanation
      %h2
        = pluralize(bbox.errors.count, "error")
        prohibited this bbox from being saved:
      %ul
        - bbox.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :title
    = form.text_field :title
  .field
    = form.label :items_per_day
    = form.number_field :items_per_day
  .actions
    = form.submit
